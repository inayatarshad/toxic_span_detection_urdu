# Training environment for URTOX / MUTEX / MUTEX-M
#
# Versions follow the configuration reported in MUTEX-M section 5:
# PyTorch 2.1, HuggingFace Transformers 4.38, torchaudio 2.1.
# Install PyTorch from the CUDA index first, then the rest:
#
#   pip install torch==2.1.2 torchaudio==2.1.2 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements-training.txt

torch==2.1.2
torchaudio==2.1.2

transformers==4.38.2
datasets==2.18.0
evaluate==0.4.1
seqeval==1.2.2

scikit-learn==1.3.2
pandas==2.1.4
numpy==1.26.4

librosa==0.10.1
pydub==0.25.1
openai-whisper==20231117

shap==0.44.1
